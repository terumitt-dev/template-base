# レビューガイドライン

## 言語
レビューコメントはすべて日本語で記述してください。

## 確認観点
以下の優先順位でレビューしてください。

1. **バグ・正確性** — ロジックの誤り、エッジケースの見落とし
2. **セキュリティ** — 脆弱性、シークレットやトークンの露出
3. **保守性** — 読みやすさ、命名の一貫性、不要な複雑さ

## 承認ポリシー
プルリクエストのレビューを依頼された場合、レビュー結果をコメントで報告してください。
正式な Approve は別の自動化ワークフローが行います。

### 重大な問題がない場合

マージをブロックすべき問題（セキュリティ脆弱性・データ破壊・クリティカルなバグ）がない場合は、
**コメントの最終行に以下の文言を一字一句そのまま記載してください。**

自動承認システムがこの文字列を正確に検索します。言い換え・変形・省略は不可です。
改善提案や軽微な指摘があっても、マージをブロックしない問題であれば必ず記載してください。

```
重大な問題は見当たりませんでした。Reviewed commit: XXXXXXXXXX
```

`XXXXXXXXXX` は必ずターミナルで `git rev-parse --short=10 HEAD` を実行して得た値で置き換えてください。
記憶や推測で書かないこと。コメント内で上記の文言を引用・言及する場合は別の表現を使ってください。

### 重大な問題がある場合

コメントで問題を指摘してください。上記の文言はコメントに含めないでください。
